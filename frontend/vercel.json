cat > ~/geraph-app/frontend/vercel.json << 'EOF'
{
  "outputDirectory": "dist/geraph-frontend/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
EOF